spec_version: 1
name: dynamic-corridor-env
type: space
runtime: fastapi
app: server.app:app
port: 8000
description: SUMO-backed emergency vehicle dynamic corridor clearing (Pune-style 4×4 signal grid, bundled as pune-5 net)
tasks:
  - id: grid_4x4_default
    difficulty: medium
    description: 4×4 signalized grid corridor (16 intersections) with background traffic, one ambulance, and seeded per-edge weights for routing
